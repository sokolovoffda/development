# TS-02 — keyof и lookup types

| | |
|--|--|
| Phase | 1 |
| Item | 2 |
| Status | todo |

Перед заданием: [конспект Item 2](../../notes/phase-1/02-keyof-lookup-types.md) · книга, Item 2.

Код: [`vue-ts-learning-project/src/labs/ts-02-keyof-lookup/`](../../vue-ts-learning-project/src/labs/ts-02-keyof-lookup/).

---

## Task

### Часть A — текст (`## Your answer`)

1. **Своими словами:** что возвращает `keyof T`?
2. Что означает `User['displayName']`?
3. Зачем в `getProperty<T, K extends keyof T>` пишут `K extends keyof T`, а не просто `key: string`?

### Часть B — код (lab)

`src/labs/ts-02-keyof-lookup/exercises.ts` — TODO 1–4.

```bash
cd vue-ts-learning-project
npm run dev
```

Активный lab: в `src/labs/active.ts` переключи на `ts-02-keyof-lookup`.

Когда готово: Status → `review` · **«проверь TS-02»**

---

## Your answer

<!-- пиши ниже этой строки -->



---

## Mentor feedback

*(заполняет ментор после проверки)*
