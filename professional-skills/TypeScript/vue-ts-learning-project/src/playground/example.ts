/**
 * Свободные эксперименты. Импортируй сюда свои файлы из этой папки.
 * Этот файл не участвует в labs — можно менять как угодно.
 */

export function playgroundPing(): string {
  return 'playground ready'
}
